name: Build and Release IPA

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          cache: true

      - name: Get Dependencies
        run: flutter pub get

      - name: Create iOS Platform Files Automatically
        run: |
          flutter create . --platforms=ios --org com.example.azawylux

      - name: Fix Info.plist Permissions
        run: |
          PLIST="ios/Runner/Info.plist"
          /usr/libexec/PlistBuddy -c "Delete :NSCameraUsageDescription" "$PLIST" || true
          /usr/libexec/PlistBuddy -c "Add :NSCameraUsageDescription string 'نحتاج الوصول إلى الكاميرا لالتقاط الصور.'" "$PLIST"
          /usr/libexec/PlistBuddy -c "Delete :NSPhotoLibraryAddUsageDescription" "$PLIST" || true
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryAddUsageDescription string 'نحتاج إذن حفظ الصور في الاستوديو.'" "$PLIST"

      - name: Build iOS IPA
        run: |
          flutter build ios --release --no-codesign

      - name: Package and Create IPA
        run: |
          mkdir -p build/ios/ipa/Payload
          if [ -d "build/ios/iphoneos/Runner.app" ]; then
            cp -r build/ios/iphoneos/Runner.app build/ios/ipa/Payload/
          elif [ -d "build/ios/iphonesimulator/Runner.app" ]; then
            cp -r build/ios/iphonesimulator/Runner.app build/ios/ipa/Payload/
          fi
          cd build/ios/ipa
          zip -r ../../../AzawyLux.ipa Payload
          cd -

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AzawyLux-IPA
          path: AzawyLux.ipa
